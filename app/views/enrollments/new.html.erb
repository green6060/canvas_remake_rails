<h1>Enroll User</h1>
<%= form_for [@course, @enrollment] do |f| %>
  <%= f.label :user %>
  <%= f.select :user_id, @users.map { |u| [u.full_name, u.id] } %>
  <br />
  <%= f.label :role %>
  <%= f.select :role, ['studnet', 'ta', 'teacher'] %>
<% end %>
